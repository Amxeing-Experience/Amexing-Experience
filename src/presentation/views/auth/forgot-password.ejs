<%
const tFn = typeof t === 'function' ? t : ((key) => key);
%><%- include('../templates/auth-layout', {
    title: tFn('pages:forgotPassword.pageTitle'),
    page: 'forgot-password',
    showAuthHeader: false,
    t: tFn,
    content: include('../molecules/auth/forgot-password-form', {
        error: error,
        success: success,
        csrfToken: csrfToken,
        showBackToLogin: true,
        supportEmail: supportEmail,
        t: tFn
    })
}) %>
