<%
const tFn = typeof t === 'function' ? t : ((key) => key);
%><%- include('../templates/auth-layout', {
    title: tFn('pages:requestAccess.pageTitle'),
    page: 'request-access',
    showAuthHeader: false,
    content: include('../molecules/auth/request-access-form', {
        csrfToken: csrfToken,
        supportEmail: supportEmail,
        formToken: typeof formToken !== 'undefined' ? formToken : '',
        t: tFn
    })
}) %>
