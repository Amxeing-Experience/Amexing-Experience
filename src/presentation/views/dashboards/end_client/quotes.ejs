<%
/**
 * Client Quotes Management Page
 * Manages client-specific quotes/cotizaciones
 *
 * Layout Structure:
 * - Direct row/column structure (no body-wrapper)
 * - Container-fluid from Flexy Bootstrap
 * - Consistent spacing and alignment
 *
 * Note: DataTables CSS/JS libraries are loaded via controller
 * Client filtering is handled server-side in QuoteController
 * Created by Denisse Maldonado
 */

const currentUserRole = typeof userRole !== 'undefined' ? userRole : 'client';
%>

<!-- Page Header with Breadcrumb -->
<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="mb-1 fw-bold">Mis Cotizaciones</h2>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-0">
                        <li class="breadcrumb-item">
                            <a href="/dashboard/end_client" class="text-decoration-none">
                                <i class="ti ti-home me-1"></i>Dashboard
                            </a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            <i class="ti ti-file-invoice me-1"></i>Cotizaciones
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<!-- Client Info Alert -->
<div class="row mb-3">
    <div class="col-12">
        <div class="alert alert-info d-flex align-items-center" role="alert">
            <i class="ti ti-info-circle me-2 fs-4"></i>
            <div>
                <strong>Vista de Cliente:</strong> Solo puede ver cotizaciones asociadas a su cuenta.
            </div>
        </div>
    </div>
</div>

<!-- Quotes Content -->
<div class="row">
    <div class="col-12">
        <%- include('../../organisms/datatable/quotes-table', {
            tableId: 'quotes-table',
            apiEndpoint: '/api/quotes',
            userRole: currentUserRole,
            showActions: true,
            endClientCaps: typeof endClientCaps !== 'undefined' ? endClientCaps : null
        }) %>
    </div>
</div>
